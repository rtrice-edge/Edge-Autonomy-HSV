<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
    <!-- Tree View -->
    <record id="view_edge_jobs_tree" model="ir.ui.view">
        <field name="name">edge.jobs.tree</field>
        <field name="model">edge.jobs</field>
        <field name="arch" type="xml">
            <tree string="Jobs">
                <field name="name"/>
                <field name="number"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_edge_jobs_form" model="ir.ui.view">
        <field name="name">edge.jobs.form</field>
        <field name="model">edge.jobs</field>
        <field name="arch" type="xml">
            <form string="Job">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="number"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_edge_jobs_search" model="ir.ui.view">
        <field name="name">edge.jobs.search</field>
        <field name="model">edge.jobs</field>
        <field name="arch" type="xml">
            <search string="Jobs">
                <field name="name"/>
                <field name="number"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_edge_jobs" model="ir.actions.act_window">
        <field name="name">Jobs</field>
        <field name="res_model">edge.jobs</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first job
            </p>
        </field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_edge_jobs"
        name="Jobs"
        action="action_edge_jobs"
        parent="purchase.menu_purchase_config"
        sequence="20"/>
    </data>
</odoo>