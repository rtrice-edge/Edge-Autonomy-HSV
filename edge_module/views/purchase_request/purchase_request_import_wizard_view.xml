<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard Form View -->
    <record id="view_purchase_request_import_wizard_form" model="ir.ui.view">
        <field name="name">purchase.request.import.wizard.form</field>
        <field name="model">purchase.request.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Import Purchase Request">
                <sheet>
                    <div class="alert alert-info" role="alert">
                        Upload an Excel file in the specified format to create a new purchase request.
                    </div>
                    <group>
                        <field name="excel_file" filename="file_name" widget="binary"/>
                        <field name="file_name" invisible="1"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_import" string="Import" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to open the wizard -->
    <record id="action_purchase_request_import_wizard" model="ir.actions.act_window">
        <field name="name">Import from Excel</field>
        <field name="res_model">purchase.request.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Add a server action button in the action menu for list view only -->
    <record id="action_import_purchase_request" model="ir.actions.server">
        <field name="name">Import from Excel</field>
        <field name="model_id" ref="model_purchase_request"/>
        <field name="binding_model_id" ref="model_purchase_request"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            action = {
                'type': 'ir.actions.act_window',
                'name': 'Import from Excel',
                'res_model': 'purchase.request.import.wizard',
                'view_mode': 'form',
                'target': 'new',
            }
        </field>
    </record>
</odoo>