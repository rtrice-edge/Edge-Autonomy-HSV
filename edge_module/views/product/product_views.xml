<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_kanban_view_custom" model="ir.ui.view">
        <field name="name">product.template.kanban.custom</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_kanban_view"/>
        <field name="arch" type="xml">
            <!-- Remove the existing price field -->
            <xpath expr="//div[hasclass('oe_kanban_details')]//strong[contains(@t-if, 'list_price')]" position="replace">
                <!-- Replace with standard_price -->
                <strong t-if="record.standard_price.value" class="o_kanban_record_price">
                    <field name="standard_price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                </strong>
            </xpath>
        </field>
    </record>
</odoo>